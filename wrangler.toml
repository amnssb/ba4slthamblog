# Cloudflare Pages 配置文件
# 文档: https://developers.cloudflare.com/pages/platform/wrangler-configuration/

name = "ham-blog"
compatibility_date = "2024-01-01"

# Pages 构建设置
[build]
command = "npm run build"

# 部署目录 - Cloudflare Pages 会自动检测 dist/ 目录
# 不需要额外的配置

# 环境变量（可选）
[env.production]
